applications:
  app1:
    type: 'golang:1.22'
    source:
      root: 'app'

    hooks:
      build: |
        go build -o bin/app .

    web:
      upstream:
        socket_family: tcp
        protocol: http
      commands:
        start: ./bin/app
      locations:
        '/':
          allow: false
          passthru: true

routes:
  "https://{default}/":
    type: upstream
    upstream: "app1:http"